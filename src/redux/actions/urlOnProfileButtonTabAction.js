export const setProfile = (label) => (dispatch) => {
    return dispatch({
        type: label
    });
};

export const setAddressLabel = (label) => (dispatch) => {
    return dispatch({
        type: label
    });
};
